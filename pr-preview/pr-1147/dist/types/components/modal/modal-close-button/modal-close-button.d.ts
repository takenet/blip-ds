import { ComponentInterface } from '../../../stencil-public-runtime';
export declare class BdsModalCloseButton implements ComponentInterface {
    /**
     * Used to hide or show the close button
     */
    active?: boolean;
    render(): any;
}
