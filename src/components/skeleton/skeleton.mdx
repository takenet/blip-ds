import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as SkeletonStories from './skeleton.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Skeleton


## Overview

Skeleton allows users to select a date or a range of dates.

<Canvas of={SkeletonStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Skeleton.

<Controls />

## Build on React

<Source of={SkeletonStories.FrameworkReact} />