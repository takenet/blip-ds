import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as UploadStories from './upload.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Upload


## Overview

Upload allows users to select a date or a range of dates.

<Canvas of={UploadStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Upload.

<Controls />

## Events

Some examples of Autocomplete Events.

<Source of={UploadStories.Events} />

## Build on React

<Source of={UploadStories.FrameworkReact} />