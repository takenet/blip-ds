import { Canvas, Meta, Controls, Source } from '@storybook/blocks';
import * as LoadingSpinnerStories from './loading-spinner.stories';

# Loading Spinner

<br></br>

## Estrutura

O componente de loading ajuda a notificar o usuário que o carregamento está em andamento.

<Canvas of={LoadingSpinnerStories.Properties}></Canvas>

<Meta isTemplate />

As seguintes propriedades são compartilhadas pelo Spinner de Carregamento.

<Controls />

<br></br>

## Usando em React

<Source of={LoadingSpinnerStories.FrameworkReact} />
