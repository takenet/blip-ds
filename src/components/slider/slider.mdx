import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as SliderStories from './Slider.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Slider

## Overview

Slider allows users to select a date or a range of dates.

<Canvas of={SliderStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Slider.

<Controls />

## Events

Some examples of Chip Slider Events.

<Source of={SliderStories.Events} />

## Build on React

<Source of={SliderStories.FrameworkReact} />
