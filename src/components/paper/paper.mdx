import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as PaperStories from './paper.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Paper


## Overview

Paper allows users to select a date or a range of dates.

<Canvas of={PaperStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Paper.

<Controls />

## Build on React

<Source of={PaperStories.FrameworkReact} />