import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as ThemeProviderStories from './theme-provider.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# ThemeProvider


## Overview

ThemeProvider allows users to select a date or a range of dates.

<Canvas of={ThemeProviderStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by ThemeProvider.

<Controls />

## Build on React

<Source of={ThemeProviderStories.FrameworkReact} />