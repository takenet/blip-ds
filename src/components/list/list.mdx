import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as ListStories from './list.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# List


## Overview

List allows users to select a date or a range of dates.

<Canvas of={ListStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by List.

<Controls />

## Events

Some examples of Autocomplete Events.

<Source of={ListStories.Events} />

## Build on React

<Source of={ListStories.FrameworkReact} />