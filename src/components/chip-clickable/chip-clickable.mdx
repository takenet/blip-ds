import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as ChipClickableStories from './chip-clickable.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Chip Clickable


## Overview

Chips are used for items that need to be labeled, categorized, or organized using keywords that describe them.

<Canvas of={ChipClickableStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Chip Clickable.

<Controls />

## Events

Some examples of Chip Clickable Events.

<Source of={ChipClickableStories.Events} />

## Build on React

<Source of={ChipClickableStories.FrameworkReact} />