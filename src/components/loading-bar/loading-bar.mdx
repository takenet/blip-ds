import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as loadingBarStories from './loading-bar.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# loadingBar


## Overview

loadingBar allows users to select a date or a range of dates.

<Canvas of={loadingBarStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by loadingBar.

<Controls />

## Build on React

<Source of={loadingBarStories.FrameworkReact} />