import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as ToastStories from './toast.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Toast


## Overview

Toast allows users to select a date or a range of dates.

<Canvas of={ToastStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Toast.

<Controls />

## Events

Some examples of Autocomplete Events.

<Source of={ToastStories.Events} />

## Build on React

<Source of={ToastStories.FrameworkReact} />