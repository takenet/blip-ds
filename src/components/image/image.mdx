import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as ImageStories from './image.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Image


## Overview

Image allows users to render image media with code.

<Canvas of={ImageStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Image.

<Controls />


## Build on React

<Source of={ImageStories.FrameworkReact} />