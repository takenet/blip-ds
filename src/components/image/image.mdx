import { Canvas, Meta, Controls, Source } from '@storybook/blocks';
import * as ImageStories from './image.stories';

# Image

<br></br>

## Estrutura

O **Image** permite que os usuários renderizem mídias de imagem com código.

<Canvas of={ImageStories.Properties}></Canvas>

<Meta isTemplate />

As seguintes propriedades são compartilhadas pelo **Image**.

<Controls />

<br></br>

## Fallback

### Imagem quebrada

Caso a o link da imagem esteja quebrado, uma ilustração para representar esse fallback será renderizado.

### Imagem não encontrada

Caso a propriedade não seja preenchida ou receba vazio, uma ilustração para representar esse fallback será renderizado.

<br></br>
## Usando em React

<Source of={ImageStories.FrameworkReact} />
