import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as DividerStories from './divider.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Divider


## Overview

Divider allows users to select a date or a range of dates.

<Canvas of={DividerStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Divider.

<Controls />

## Build on React

<Source of={DividerStories.FrameworkReact} />