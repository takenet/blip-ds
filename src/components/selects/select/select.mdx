import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as SelectStories from './select.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Select

## Overview

Select allows users to select a date or a range of dates.

<Canvas of={SelectStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Select.

<Controls />

## Events

Some examples of Autocomplete Events.

<Source of={SelectStories.Events} />

## Build on React

<Source of={SelectStories.FrameworkReact} />