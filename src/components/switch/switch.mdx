import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as SwitchStories from './switch.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Switch


## Overview

Switch allows users to select a date or a range of dates.

<Canvas of={SwitchStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Switch.

<Controls />

## Events

Some examples of Autocomplete Events.

<Source of={SwitchStories.Events} />

## Build on React

<Source of={SwitchStories.FrameworkReact} />