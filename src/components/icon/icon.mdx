import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as IconStories from './icon.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Icon


## Overview

Icon allows users to select a date or a range of dates.

<Canvas of={IconStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Icon.

<Controls />


## Build on React

<Source of={IconStories.FrameworkReact} />