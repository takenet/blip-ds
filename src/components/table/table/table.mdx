import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as TableStories from './table.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Table


## Overview

Table allows users to select a date or a range of dates.

<Canvas of={TableStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by Table.

<Controls />

## Build on Web Component

<Source of={TableStories.WebComponent} />

## Build on React

<Source of={TableStories.FrameworkReact} />