import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as DataTableStories from './data-table.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# DataTable


## Overview

DataTable allows users to select a date or a range of dates.

<Canvas of={DataTableStories.Properties}></Canvas>

<Meta isTemplate />

The following properties that are shared by DataTable.

<Controls />

## Build on React

<Source of={DataTableStories.FrameworkReact} />