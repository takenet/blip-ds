import { Story, ArgsTable, Canvas } from '@storybook/blocks';
import * as ProgressBarStories from './progress-bar.stories';
import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';

# Barra de Progresso

<br></br>

## Visão Geral

O componente Barra de Progresso permite exibir o progresso de uma tarefa ou processo.

<Canvas of={ProgressBarStories.Properties}></Canvas>

<Meta isTemplate />

<br></br>

As propriedades a seguir são compartilhadas pelo componente Barra de Progresso.

<Controls />

<br></br>

## Usando no React

<Source of={ProgressBarStories.FrameworkReact} />
